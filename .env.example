# Local (SQLite) — développement et vitrine GitHub Pages
DATABASE_URL="file:./dev.db"

# Production (Vercel) — utilisez une base Postgres hébergée (Neon / Vercel Postgres)
# DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# Secret de session (obligatoire en production) : openssl rand -base64 32
AUTH_SECRET="changez-moi-en-production"
